<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mobil.Views.Resumen"
             Title="Resumen Reserva">

    <ListView x:Name="ListFuncion">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid>                               
                            <StackLayout Orientation="Vertical">
                                <Label Text="{Binding Cinema}" FontAttributes="Bold" FontSize="15" AnchorX="4"/>
                                <Label Text="{Binding Sala}" FontSize="13"/>
                                <Label Text="{Binding Ciudad}" FontSize="13" />
                                <Label Text="{Binding Precio}" FontSize="13" />
                            </StackLayout >                     
                    </Grid>
                </ViewCell>

            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>